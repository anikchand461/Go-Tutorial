# Comma Ok Idiom and Map Operations Questions

### **Question 1: Use Comma Ok Idiom to Check for Missing Keys**
In the code, the `comma ok` idiom is used to check if a key exists in the map. 
Modify the program to check if the key "goodbye" exists in the map. 
If it doesn't exist, print "Key not found". If it exists, print the value.

#### Input:
- A map with string keys and integer values.

#### Output:
- A message indicating whether the key "goodbye" exists, or "Key not found" if it doesn't.

---

### **Question 2: Delete Key from Map**
After checking the value of the key "hello", the program deletes the key "hello" from the map. 
Modify the program to delete a different key from the map (e.g., "world") and then check the value of the deleted key using the comma ok idiom.

#### Input:
- A map with string keys and integer values.

#### Output:
- The result after deleting the "world" key and checking if it's still present.

---

### **Question 3: Create a Set-like Map with Strings**
Create a map `strSet` that simulates a set of strings. Add at least 5 string values, including duplicates, and print the map after adding the values.

#### Input:
- A map with string keys and boolean values (simulating a set).

#### Output:
- The set-like map with unique strings printed.

---

### **Question 4: Handle Missing Keys with Zero Value**
Modify the code to handle a scenario where the key "java" is not found in the map, and instead of printing the zero value, print a custom message such as "Key 'java' not found in the map".

#### Input:
- A map with string keys and integer values.

#### Output:
- The value associated with "java", or a message indicating it was not found.

---

### **Question 5: Checking Membership in a Set-like Map**
In the code, the program checks if a value exists in the set-like map `intSet`. 
Add another condition to check whether the number `6` exists in `intSet` and print the result ("6 is in the set" or "6 is not in the set").

#### Input:
- A set-like map with integer keys and boolean values.

#### Output:
- A message indicating whether the value `6` is in the set-like map.