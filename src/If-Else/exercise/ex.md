# 10 Coding Problems - If-Else and Scoping in Go

## Problem 1: Random Number Categorization
Write a program that generates a random integer between 1 and 10. Using `if-else` statements, categorize the number as:
- "Too low" if the number is less than 3,
- "Just right" if the number is between 3 and 7,
- "Too high" if the number is greater than 7.

### Input:
- None (the number is randomly generated).

### Output:
- The appropriate message based on the random number.

---

## Problem 2: Name Check
Write a program that checks if the user has entered their name. If the name is empty, print "No Name Entered", otherwise print "Hello, [name]".

### Input:
- A string (name).

### Output:
- If the name is empty, print "No Name Entered". Otherwise, print "Hello, [name]".

---

## Problem 3: Temperature Check
Write a program that generates a random temperature between -10 and 40 degrees Celsius. Use `if-else` statements to categorize the temperature as:
- "Freezing" if the temperature is less than 0,
- "Cold" if the temperature is between 0 and 15,
- "Warm" if the temperature is between 15 and 30,
- "Hot" if the temperature is above 30.

### Input:
- None (the temperature is randomly generated).

### Output:
- The appropriate message based on the temperature.

---

## Problem 4: Random Number Guessing
Write a program that generates a random number between 1 and 100 and asks the user to guess it. If the guess is:
- Correct, print "You guessed it right!",
- Too high, print "Try a smaller number",
- Too low, print "Try a larger number".

### Input:
- A single integer (the user's guess).

### Output:
- Print the appropriate message based on the user's guess.

---

## Problem 5: Check If Number is Positive or Negative
Write a program that asks the user to input a number. Use `if-else` to check if the number is positive, negative, or zero, and print the result accordingly.

### Input:
- A single integer.

### Output:
- "Positive" if the number is greater than 0,
- "Negative" if the number is less than 0,
- "Zero" if the number is 0.

---

## Problem 6: Leap Year Check
Write a program that asks the user to input a year and checks whether it is a leap year. A year is a leap year if:
- It is divisible by 4,
- If it is divisible by 100, it must also be divisible by 400.

### Input:
- A single integer (year).

### Output:
- Print "Leap Year" if the year is a leap year, otherwise print "Not a Leap Year".

---

## Problem 7: Eligibility Check for Voting
Write a program that checks if the user is eligible to vote. A person is eligible to vote if their age is 18 or more.

### Input:
- A single integer (age).

### Output:
- "Eligible to vote" if the age is 18 or above,
- "Not eligible to vote" if the age is below 18.

---

## Problem 8: Calculate Discount Based on Age
Write a program that checks the user's age and offers a discount:
- 20% discount for ages between 18 and 30,
- 10% discount for ages between 31 and 50,
- No discount for ages above 50.

### Input:
- A single integer (age).

### Output:
- Print the appropriate discount based on the user's age.

---

## Problem 9: Random Number Range Check
Write a program that generates a random number between 1 and 50. Use `if-else` to check if the number falls into any of the following ranges:
- 1-10, print "Low range",
- 11-30, print "Medium range",
- 31-50, print "High range".

### Input:
- None (the number is randomly generated).

### Output:
- The appropriate message based on the random number's range.

---

## Problem 10: Grade Classification
Write a program that asks the user for their marks and classifies the grade as:
- "A" for marks 90 and above,
- "B" for marks between 70 and 89,
- "C" for marks between 50 and 69,
- "F" for marks below 50.

### Input:
- A single integer (marks).

### Output:
- Print the grade ("A", "B", "C", or "F") based on the user's marks.